import random as r

print(r.randrange(4)) #it will give random value only positive int values
print(r.randint(1,5))  #it will generate random values between 1 and 5, only int values
print(r.random())  # it will generate randon fractional values
print(r.randbytes(4))  #generates random aplha values
print(r.randrange(1,5))